using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DLTEST : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       //Debug.Log(DL.GetDistance().dic);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
